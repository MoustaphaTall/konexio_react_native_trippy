import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const Hotel = ({ details }) => {
    console.log("cmp/hotel/hotel", details);

    return (
        <Text>Hotel</Text>
    );
}

export default Hotel;